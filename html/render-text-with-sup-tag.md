---
title: "Usar `<sup>` para renderizar sobrescrito"
author: "bw3sley"
slug: "render-text-with-sup-tag"
tags:
  - html
  - semantics
  - text
created_at: "2026-07-05"

---

# Usar `<sup>` para renderizar sobrescrito

Quando o texto precisa aparecer como expoente ou sobrescrito, use a tag semântica `<sup>`.

```html
<p>x<sup>2</sup></p>
```

// Renderiza: x²
