---
title: "Usar `classList.contains()` para verificar classe em elemento DOM"
author: "bw3sley"
slug: "check-dom-classes-with-classlist"
tags:
  - javascript
  - dom
  - classes
created_at: "2026-07-05"

---

# Usar `classList.contains()` para verificar classe em elemento DOM

`classList.contains()` verifica se um elemento possui uma classe específica sem precisar inspecionar a string de `class` manualmente.

```js
element.classList.contains("hidden");
// true
```
